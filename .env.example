# Variables d'environnement pour docker-compose (VPS / prod)
# Copier en .env sur le VPS et remplir les valeurs reelles.
# .env est ignore par git (jamais commit).

# Base de donnees Postgres
DB_USER=seomonitor
DB_PASSWORD=change_me_to_a_strong_password
DB_NAME=seomonitor

# Stripe (cle publique - frontend)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_xxxxx
